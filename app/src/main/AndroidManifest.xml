<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ocean.restapicallretrofit">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyAppRestApiCallRetrofit">
        <activity
            android:name=".users.UsersDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".todos.TodoActivity"
            android:exported="false" />
        <activity
            android:name=".photos.PhotoActivity"
            android:exported="false" />
        <activity
            android:name=".albums.AlbumsActivity"
            android:exported="false" />
        <activity
            android:name=".comments.CommentsActivity"
            android:exported="false" />
        <activity
            android:name=".posts.ActivityPost"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>